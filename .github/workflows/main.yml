name: A workflow for my Hello World file
on: push
jobs:
  build:
    name: Hello world action
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - uses: dawidh15/hello-github-actions/action-a/action
        with:
          MY_NAME: "Mona"
